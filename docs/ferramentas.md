# Ferramentas

SINTEGRA: PE expõe 1 ferramenta (todas somente leitura).

### 1. `sintegra_pe_consultar`
**Input**: `cnpj` (opcional), `ie` (opcional), `cpf` (opcional), `login_cpf` (opcional), `login_senha` (opcional), `pkcs12_cert` (opcional), `pkcs12_pass` (opcional)

SINTEGRA: PE, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar SINTEGRA: PE
```
